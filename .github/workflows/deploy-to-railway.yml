# Note: This workflow is disabled. Railway auto-deploys via GitHub integration.
# The project is connected at https://railway.app/project/fda2073b-d325-4734-8dd6-20deb81eb585
# All pushes to main branch automatically trigger deployments through Railway's native integration.

name: Deploy to Railway (Disabled - Using Native Integration)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Deployment Status
        run: |
          echo "✅ Code pushed to GitHub"
          echo "🚀 Railway will auto-deploy via native GitHub integration"
          echo "📝 Check deployment status at: https://railway.app/project/fda2073b-d325-4734-8dd6-20deb81eb585"
